skills:
  directory: "skills"
  
  enabled_skills:
    - test-execution
    - playwright-cli
    - kb-query
  
  skill_loading:
    max_loaded_skills: 3

  self_improve:
    nudge_interval: 30
    background_review:
      enabled: false
      max_iterations: 8
