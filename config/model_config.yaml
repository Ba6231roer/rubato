model:
  provider: "openai"
  name: "gpt-4"
  api_key: "${OPENAI_API_KEY}"
  base_url: null
  temperature: 0.7
  max_tokens: 2000

fallback_model:
  provider: "anthropic"
  name: "claude-3-sonnet"
  api_key: "${ANTHROPIC_API_KEY}"

parameters:
  retry_times: 3
  retry_delay: 1.0
  timeout: 30.0
